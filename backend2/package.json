{
  "name": "backend2",
  "version": "1.0.0",
  "description": "backend server for spin n stone",
  "scripts": {
    "build": "npx tsc && pushd .. && npm run build && popd",
    "start": "node build",
    "start-dev": "nodemon build",
    "dev": "npx tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "zeta"
  ],
  "author": "team zeta",
  "license": "WTFPL",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "ts-postgres": "^1.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.11",
    "typescript": "^5.0.4"
  }
}
